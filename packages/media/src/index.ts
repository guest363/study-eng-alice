/**
 * @tw/media — звуковой слой: TTS через speechSynthesis (выбор голоса, темп,
 * очередь), плеер файлов, самозапись через MediaRecorder. Реализация — гейт G2.
 *
 * Правила браузеров: автовоспроизведение только после первого тапа,
 * getVoices() асинхронен (см. agent.md, «Известные ловушки»).
 */
export {};
