/**
 * Акцент озвучки: en-US и en-GB (task.md RF-10.1). Реакция Рассеивание переключает
 * акцент на один день, а ребёнок выбирает любимый в настройках родителя.
 */

/** Тип голоса, как его понимает контракт слов и настройки. */
export type Accent = "us" | "uk";

/** Подписи акцентов для настроек — тоже текст игры, он придёт из контента в G7. */
export const ACCENT_IDS = ["us", "uk"] as const;

/** Темп для новых слов (RF-10.1): медленно, чтобы ребёнок успевал. */
export const NEW_WORD_RATE = 0.85;

/** Темп «медленно» в упражнениях echo-sound (RF-4.4). */
export const SLOW_RATE = 0.7;
